function replaceTextSecondaryFallback() {
    console.log("If a text has color: #64748b, and we need 4.5 contrast... ")
    // wait, I need to find all #64748b that are used for text and replace them with var(--text-secondary, #64748b).
    // What about #475569? Same thing.
}
