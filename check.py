# In origin/main:internal.html, `generate-report-btn` was already present as display:none, and the actual active button was `btn-generate-report`!
# My synced block includes `<button id="generate-report-btn" class="filter-btn active" ...>` which now ALSO shows up.
# So I have TWO "📊 Bericht generieren (PDF)" buttons rendered right next to each other!
# Let's check `internal.html` to see if both are visible.
