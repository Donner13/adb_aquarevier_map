print("As per memory: Playwright UI regression tests (e.g., in layer-toggles.spec.js) are prone to timeouts for 'all layers ON/OFF' and image snapshot failures due to viewport dimension mismatches (e.g., receiving 638px by 720px instead of expected 840px by 720px) caused by sandbox infra limitations. Report these as existing environment limitations rather than attempting to debug them or expanding the PR scope.")
